server {
  listen 80;
  root /usr/share/nginx/html;
  index index.html;

  # SPA fallback: mọi URL không khớp file → trả index.html
  # → React Router xử lý route phía client
  location / {
    try_files $uri /index.html;
  }
}